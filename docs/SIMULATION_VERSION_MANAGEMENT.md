# Production Plan Simulation Version Management
**生产计划模拟版本管理**

**版本**: v1.0
**创建日期**: 2026-01-29
**状态**: 需求定义 + 待实现

---

## 📋 功能概述

### 核心需求

用户在生成 Production Plan 时，希望能够：
1. **保存多个 Simulation 版本**（不直接覆盖 POR）
2. **横向对比不同 Simulation**（找出最优方案）
3. **将 Simulation 转为 POR**（一键升级）
4. **查看 POR 版本历史**（追溯变更）
5. **对比 POR 版本差异**（关键参数对比）

---

## 🗂️ 版本类型定义

### **1. SIMULATION（模拟版本）**
```
定义：
- 用户针对不同假设条件生成的临时方案
- 可以有多个并存
- 不影响实际生产计划（POR）

特点：
- 易创建、易删除
- 支持命名（如"春节高峰应对方案 A"）
- 支持批量对比

生命周期：
SIMULATION → (用户选择) → POR
     ↓
   删除（不再需要）
```

### **2. POR (Plan of Record)**
```
定义：
- 正式的生产计划版本
- 当前只有 1 个 "Active POR"
- 历史 POR 保留用于审计和对比

特点：
- 由 SIMULATION 转化而来
- 版本号自动递增（POR v1.0, v1.1, v2.0...）
- 记录创建时间、创建条件、变更原因

生命周期：
POR v1.0 → (新 Simulation 转 POR) → POR v2.0 (v1.0 归档)
```

---

## 🎨 UI/UX 设计

### **页面结构**

#### **Production Plan 主页面**
```
┌─────────────────────────────────────────────────┐
│ Production Plan                                  │
├─────────────────────────────────────────────────┤
│                                                  │
│  Tab 1: Generate New Simulation  ⭐ 默认         │
│  Tab 2: Simulation Library       📚             │
│  Tab 3: POR (Plan of Record)     ✅             │
│  Tab 4: POR Version History      📜             │
│                                                  │
└─────────────────────────────────────────────────┘
```

---

### **Tab 1: Generate New Simulation** ⭐

**现有功能保持不变**，但修改按钮文字和行为：

```
修改前：
[Generate New Production Plan]
  → 直接生成并覆盖到 "Latest Plan"

修改后：
[Generate New Simulation]
  → 弹出 Modal：
     ┌─────────────────────────────────────┐
     │ Save Simulation                      │
     ├─────────────────────────────────────┤
     │ Name: [春节高峰应对方案 A_______]    │
     │                                      │
     │ Description (Optional):              │
     │ [假设：CTB 充足，启用周日 OT____]    │
     │                                      │
     │ Tags (Optional):                     │
     │ [x] High OT   [x] CTB Constrained   │
     │                                      │
     │     [Cancel]  [Save Simulation]     │
     └─────────────────────────────────────┘

保存后：
  → 添加到 Simulation Library
  → 自动打开新窗口显示报表
  → 主窗口跳转到 "Simulation Library" Tab
```

---

### **Tab 2: Simulation Library** 📚

**布局**：卡片式网格布局

```
┌─────────────────────────────────────────────────────────────┐
│  🔍 Search: [_____________]  Filter: [All / Constrained / ...]│
│  Sort by: [Created Date ▼]                                   │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐│
│  │ 📊 Simulation 3│  │ 📊 Simulation 2│  │ 📊 Simulation 1││
│  ├────────────────┤  ├────────────────┤  ├────────────────┤│
│  │ 春节高峰 A      │  │ 标准排产方案    │  │ CTB短缺应对   ││
│  │                │  │                │  │                ││
│  │ Mode: Combined │  │ Mode: Constr.  │  │ Mode: Unconstr││
│  │ Period: 10/1-31│  │ Period: 10/1-31│  │ Period: 10/1-31││
│  │ Created: 1/29  │  │ Created: 1/28  │  │ Created: 1/28 ││
│  │                │  │                │  │                ││
│  │ Gap Summary:   │  │ Gap Summary:   │  │ Gap Summary:   ││
│  │ ✅ Week 1: +200 │  │ ⚠️ Week 2:-500 │  │ ✅ All Weeks OK││
│  │ ⚠️ Week 2: -100 │  │ ✅ Week 3: +100│  │                ││
│  │                │  │                │  │                ││
│  │ [View Report]  │  │ [View Report]  │  │ [View Report]  ││
│  │ [Compare]      │  │ [Compare]      │  │ [Compare]      ││
│  │ [→ Convert to  │  │ [→ Convert to  │  │ [→ Convert to  ││
│  │    POR]        │  │    POR]        │  │    POR]        ││
│  │ [Delete]       │  │ [Delete]       │  │ [Delete]       ││
│  └────────────────┘  └────────────────┘  └────────────────┘│
│                                                              │
│  [+ Generate New Simulation]                                 │
└─────────────────────────────────────────────────────────────┘
```

**功能**：
- ✅ 显示所有 Simulation 版本
- ✅ 卡片展示关键信息：名称、模式、时间范围、Gap 摘要
- ✅ 快速操作：查看、对比、转 POR、删除
- ✅ 支持搜索和筛选

---

### **Tab 3: POR (Plan of Record)** ✅

**当前 Active POR 展示**

```
┌─────────────────────────────────────────────────────────────┐
│  📌 Current POR (Plan of Record)                             │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ POR Version: v2.3                                     │  │
│  │ Generated: 2026-01-28 14:30                          │  │
│  │ Promoted from: "春节高峰应对方案 A" (Simulation)      │  │
│  ├──────────────────────────────────���───────────────────┤  │
│  │ Configuration:                                        │  │
│  │   • Mode: Constrained (CTB Applied)                  │  │
│  │   • Period: 2026-10-01 to 2026-10-31                 │  │
│  │   • Sites: WF, VN02                                  │  │
│  │   • Ramp: Standard (30 days)                         │  │
│  │   • OT: Sunday OT Enabled                            │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │ Key Metrics:                                          │  │
│  │   • Total Output: 128,400 units                      │  │
│  │   • Total Shipment: 125,000 units                    │  │
│  │   • Overall Attainment: 98.5%                        │  │
│  │   • Weeks with Gap < 0: Week 2 (-500 units)          │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │ Changes from Previous POR (v2.2):                     │  │
│  │   🔄 Mode: Unconstrained → Constrained               │  │
│  │   🔄 OT: Disabled → Sunday OT Enabled                │  │
│  │   📊 Output: 135,000 → 128,400 (-6,600, -4.9%)       │  │
│  │   📊 Attainment: 100% → 98.5% (-1.5pp)               │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │ [📄 View Full Report]  [📊 Compare with v2.2]        │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**功能**：
- ✅ 显示当前正式生产计划
- ✅ 展示生成条件（Configuration）
- ✅ 关键指标摘要
- ✅ **自动对比上一版 POR 的差异** ⭐ 核心需求

---

### **Tab 4: POR Version History** 📜

**历史 POR 版本列表**

```
┌─────────────────────────────────────────────────────────────┐
│  📜 POR Version History                                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ ✅ POR v2.3 (Current)           Created: 2026-01-28  │  │
│  │    Changes: +Sunday OT, CTB Constrained              │  │
│  │    [View Report] [Compare with v2.2]                 │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ POR v2.2                        Created: 2026-01-20  │  │
│  │    Changes: Ramp Fast → Standard                     │  │
│  │    [View Report] [Compare with v2.3]                 │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ POR v2.1                        Created: 2026-01-15  │  │
│  │    Changes: Added VN02 site                          │  │
│  │    [View Report] [Compare with v2.2]                 │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 核心功能设计

### **功能 1: 保存 Simulation**

**流程**：
```
1. 用户点击 "Generate New Simulation"
2. 选择 Planning Mode (Unconstrained / Constrained / Combined)
3. 系统生成计划数据
4. 弹出 "Save Simulation" Modal
   - 输入名称（必填）
   - 输入描述（可选）
   - 选择标签（可选）
5. 点击 "Save Simulation"
6. 系统保存到 localStorage (短期) / 数据库 (长期)
   - Key: 'simulation_' + timestamp
   - Value: { name, description, config, results, createdAt }
7. 跳转到 Simulation Library
8. 同时打开新窗口显示报表
```

**数据结构**：
```javascript
{
  id: 'sim_20260129_143025',
  type: 'SIMULATION',
  name: '春节高峰应对方案 A',
  description: '假设：CTB 充足，启用周日 OT',
  tags: ['High OT', 'CTB Constrained'],

  config: {
    mode: 'constrained',
    dateRange: { start: '2026-10-01', end: '2026-10-31' },
    sites: ['WF', 'VN02'],
    rampCurve: 'standard',
    otEnabled: true,
    // ... 完整配置
  },

  results: {
    programResults: [ /* daily data */ ],
    weeklyMetrics: [ /* weekly data */ ],
    siteResults: { /* site breakdown */ },
    summary: {
      totalOutput: 128400,
      totalShipment: 125000,
      overallAttainment: 98.5,
      weeksWithGap: ['2026-W42']
    }
  },

  createdAt: '2026-01-29T14:30:25Z',
  createdBy: 'user@example.com'
}
```

---

### **功能 2: Simulation 横向对比**

**场景**：用户想对比 2-3 个 Simulation，看哪个方案最优

**实现方式**：

#### **Step 1: 选择要对比的版本**
```
在 Simulation Library 页面：
- 用户勾选 2-3 个 Simulation
- 点击 "Compare Selected" 按钮
```

#### **Step 2: 并排展示对比**
```
┌────────────────────────────────────────────────────────────┐
│  Compare Simulations                                        │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┬─────────────┬─────────────┐              │
│  │ Sim A       │ Sim B       │ Sim C       │              │
│  │ 春节高峰 A  │ 标准排产    │ CTB短缺应对 │              │
│  ├─────────────┼─────────────┼─────────────┤              │
│  │ Config:     │ Config:     │ Config:     │              │
│  │ • Constr.   │ • Unconstr. │ • Constr.   ��              │
│  │ • OT: Yes   │ • OT: No    │ • OT: Yes   │              │
│  ├─────────────┼─────────────┼─────────────┤              │
│  │ Output:     │ Output:     │ Output:     │              │
│  │ 128,400     │ 135,000 ⬆️  │ 120,000 ⬇️  │              │
│  ├─────────────┼─────────────┼─────────────┤              │
│  │ Attainment: │ Attainment: │ Attainment: │              │
│  │ 98.5%       │ 100% ⬆️     │ 95.0% ⬇️    │              │
│  ├─────────────┼─────────────┼─────────────┤              │
│  │ Weeks Gap<0:│ Weeks Gap<0:│ Weeks Gap<0:│              │
│  │ 1 (W42)     │ 0 ✅        │ 2 (W42,W43) │              │
│  └─────────────┴─────────────┴─────────────┘              │
│                                                             │
│  Weekly Gap Comparison Chart:                              │
│  ┌────────────────────────────────────────────┐           │
│  │     Week  │  Sim A  │  Sim B  │  Sim C     │           │
│  │    W40    │  +200   │  +500   │  +100      │           │
│  │    W41    │  +150   │  +300   │   -50      │           │
│  │    W42    │  -100🔴 │    0    │  -300🔴    │           │
│  │    W43    │  +100   │  +200   │  -150🔴    │           │
│  └────────────────────────────────────────────┘           │
│                                                             │
│  Recommendation:                                            │
│  🏆 Sim B (标准排产) has the best overall attainment      │
│     but may not be realistic due to CTB constraints.       │
│  ⭐ Sim A (春节高峰 A) is recommended if CTB is limited.  │
│                                                             │
└────────────────────────────────────────────────────────────┘
```

**对比维度**：
- ✅ 配置差异（Mode, OT, Ramp Curve）
- ✅ 总体指标（Output, Shipment, Attainment）
- ✅ Weekly Gap 逐周对比
- ✅ 风险评估（有多少周 Gap < 0）
- ✅ 推荐意见（基于规则引擎）

---

### **功能 3: Simulation 转 POR** ⭐ 核心功能

**流程**：
```
1. 用户在 Simulation Library 选中一个 Simulation
2. 点击 "Convert to POR" 按钮
3. 弹出确认 Modal：
   ┌─────────────────────────────────────┐
   │ Promote to POR                       │
   ├─────────────────────────────────────┤
   │ Are you sure you want to promote:    │
   │                                      │
   │ "春节高峰应对方案 A"                 │
   │                                      │
   │ to the new Plan of Record?           │
   │                                      │
   │ This will replace the current POR:   │
   │ POR v2.2 → POR v2.3                  │
   │                                      │
   │ Notes (Optional):                    │
   │ [应对春节高峰需求，启用周日OT___]    │
   │                                      │
   │     [Cancel]  [Confirm Promotion]   │
   └─────────────────────────────────────┘

4. 确认后：
   - 当前 POR (v2.2) 归档到 History
   - Simulation 转为新 POR (v2.3)
   - 自动生成 "Changes from Previous POR" 报告
   - 跳转到 "POR" Tab 显示新 POR

5. 新 POR 页面显示：
   - 配置信息
   - 关键指标
   - **与上一版 POR 的差异** ⭐
```

**POR 版本号规则**：
```
格式：v<major>.<minor>

Major 变更（+1.0）：
- 日期范围变化 > 7 天
- Sites 增加/减少
- Mode 从 Unconstrained ↔ Constrained

Minor 变更（+0.1）：
- OT 开关
- Ramp Curve 调整
- 小幅参数调整

示例：
v2.2 → v2.3 (Minor: 启用 OT)
v2.3 → v3.0 (Major: 增加新站点 VN02)
```

---

### **功能 4: POR 版本对比**

**对比内容**：

#### **4.1 Configuration 对比**
```
┌────────────────────────────────────────────────┐
│ Configuration Changes: POR v2.2 → v2.3         │
├────────────────────────────────────────────────┤
│ Parameter          │ v2.2        │ v2.3        │
│────────────────────┼─────────────┼─────────────│
│ Mode               │ Unconstr.   │ Constr. 🔄  │
│ Date Range         │ 10/1-10/31  │ 10/1-10/31  │
│ Sites              │ WF          │ WF, VN02 🆕 │
│ Ramp Curve         │ Standard    │ Standard    │
│ Sunday OT          │ ❌ Disabled  │ ✅ Enabled 🔄│
│ Holiday OT         │ ❌ Disabled  │ ❌ Disabled  │
└────────────────────────────────────────────────┘
```

#### **4.2 Summary Metrics 对比**
```
┌────────────────────────────────────────────────┐
│ Summary Metrics Changes: POR v2.2 → v2.3       │
├────────────────────────────────────────────────┤
│ Metric             │ v2.2        │ v2.3        │ Δ       │
│────────────────────┼─────────────┼─────────────┼─────────│
│ Total Output       │ 135,000     │ 128,400     │ -4.9% 📉│
│ Total Shipment     │ 130,000     │ 125,000     │ -3.8% 📉│
│ Total Demand       │ 130,000     │ 127,000     │ -2.3%   │
│ Overall Attainment │ 100%        │ 98.4%       │ -1.6pp 📉│
│ Weeks Gap < 0      │ 0           │ 1 (W42)     │ +1 ⚠️   │
└────────────────────────────────────────────────┘
```

#### **4.3 Weekly Gap 逐周对比**
```
┌────────────────────────────────────────────────┐
│ Weekly Gap Comparison: POR v2.2 vs v2.3        │
├────────────────────────────────────────────────┤
│ Week  │ v2.2 Gap │ v2.3 Gap │ Δ Gap   │ Status │
│───────┼──────────┼──────────┼─────────┼────────│
│ W40   │  +500    │  +200    │  -300   │ 🟡 Worse│
│ W41   │  +300    │  +150    │  -150   │ 🟡 Worse│
│ W42   │    0     │  -100    │  -100   │ 🔴 WORSE│
│ W43   │  +200    │  +100    │  -100   │ 🟡 Worse│
└────────────────────────────────────────────────┘

Analysis:
⚠️ v2.3 has lower output due to CTB constraints
🔴 Week 42 now has a shortage (-100 units)
💡 Recommendation: Consider Holiday OT for Week 42
```

#### **4.4 待完善的对比维度** 🔄

**当前暂时搁置，未来完善**：
```
待对比的方面（占位）：

□ Cost Analysis（成本对比）
  - Labor cost (OT vs Regular)
  - Material cost
  - Total manufacturing cost
  - Cost per unit

□ Resource Utilization（资源利用率）
  - Line utilization %
  - Labor headcount changes
  - Inventory levels (WIP, FG)

□ Risk Assessment（风险评估）
  - CTB shortage weeks
  - Labor shortage weeks
  - Quality risk (Ramp too fast?)
  - Schedule risk (Can we recover gaps?)

□ Sustainability Metrics（可持续性）
  - OT hours (CSR compliance)
  - Carbon footprint (if data available)

□ Scenario What-If（情景分析）
  - What if Demand +10%?
  - What if CTB delivery delayed 1 week?
  - What if Yield drops to 85%?
```

**实现方式**：
- 在 POR 对比页面预留 "Advanced Comparison" 折叠区域
- 显示 "Coming Soon" 占位
- 未来根据需求优先级逐步实现

---

## 💾 数据存储设计

### **短期方案（MVP）：localStorage**

```javascript
// Simulations
localStorage.setItem('simulations', JSON.stringify([
  { id: 'sim_001', name: '...', config: {...}, results: {...} },
  { id: 'sim_002', name: '...', config: {...}, results: {...} }
]));

// Current POR
localStorage.setItem('currentPOR', JSON.stringify({
  id: 'POR_v2.3',
  version: 'v2.3',
  promotedFrom: 'sim_001',
  config: {...},
  results: {...},
  createdAt: '...',
  changesFromPrevious: { /* diff */ }
}));

// POR History
localStorage.setItem('porHistory', JSON.stringify([
  { id: 'POR_v2.3', version: 'v2.3', ... },
  { id: 'POR_v2.2', version: 'v2.2', ... },
  { id: 'POR_v2.1', version: 'v2.1', ... }
]));
```

**优点**：
- ✅ 快速实现，无需后端
- ✅ 适合 MVP 验证

**缺点**：
- ❌ 容量限制（5-10MB）
- ❌ 无法多用户协作
- ❌ 浏览器清理后数据丢失

---

### **长期方案：PostgreSQL**

参考前面的数据库架构设计。

---

## 🎯 实现优先级

### **Phase 1: MVP（本周完成）** ⭐
- [x] 修改 "Generate" 流程，保存为 Simulation
- [x] Simulation Library 页面（卡片展示）
- [x] Simulation 转 POR 功能
- [x] POR 展示页面（含配置和关键指标）
- [x] POR 与上一版的基本对比（Config + Summary）

**预计工作量**: 10-12 小时

---

### **Phase 2: 对比增强（下周完成）**
- [ ] Simulation 横向对比（2-3 个并排）
- [ ] Weekly Gap 逐周对比表格
- [ ] 对比可视化（Gap 折线图）
- [ ] POR History 页面

**预计工作量**: 8-10 小时

---

### **Phase 3: 高级功能（2-3 周后）**
- [ ] 搜索和筛选 Simulation
- [ ] 导出对比报告（Excel/PDF）
- [ ] 版本标签系统（Tag Management）
- [ ] 批量删除 Simulation
- [ ] localStorage 清理机制（保留最近 20 个）

**预计工作量**: 6-8 小时

---

### **Phase 4: 数据库迁移（1-2 个月后）**
- [ ] PostgreSQL 数据库搭建
- [ ] API 后端开发
- [ ] 数据迁移工具
- [ ] 多用户权限管理

**预计工作量**: 40-50 小时

---

## 📝 技术实现笔记

### **关键函数设计**

```javascript
// 1. 保存 Simulation
function saveSimulation(name, description, config, results) {
  const simulation = {
    id: 'sim_' + Date.now(),
    type: 'SIMULATION',
    name,
    description,
    config,
    results,
    createdAt: new Date().toISOString()
  };

  const simulations = getSimulations();
  simulations.push(simulation);
  localStorage.setItem('simulations', JSON.stringify(simulations));

  return simulation.id;
}

// 2. 获取所有 Simulations
function getSimulations() {
  const data = localStorage.getItem('simulations');
  return data ? JSON.parse(data) : [];
}

// 3. Simulation 转 POR
function promoteSimulationToPOR(simulationId, notes) {
  const simulation = getSimulations().find(s => s.id === simulationId);
  if (!simulation) throw new Error('Simulation not found');

  // 归档当前 POR
  const currentPOR = getCurrentPOR();
  if (currentPOR) {
    archivePOR(currentPOR);
  }

  // 创建新 POR
  const newVersion = getNextPORVersion();
  const newPOR = {
    id: 'POR_' + newVersion.replace('.', '_'),
    version: newVersion,
    type: 'POR',
    promotedFrom: simulationId,
    name: simulation.name,
    config: simulation.config,
    results: simulation.results,
    createdAt: new Date().toISOString(),
    notes,
    changesFromPrevious: currentPOR ? comparePORs(currentPOR, simulation) : null
  };

  localStorage.setItem('currentPOR', JSON.stringify(newPOR));

  return newPOR;
}

// 4. 对比两个 POR
function comparePORs(porA, porB) {
  return {
    configChanges: compareConfigs(porA.config, porB.config),
    summaryChanges: compareSummaries(porA.results.summary, porB.results.summary),
    weeklyGapChanges: compareWeeklyGaps(porA.results.weeklyMetrics, porB.results.weeklyMetrics)
  };
}

// 5. 对比多个 Simulations
function compareSimulations(simulationIds) {
  const simulations = simulationIds.map(id =>
    getSimulations().find(s => s.id === id)
  );

  return {
    configs: simulations.map(s => s.config),
    summaries: simulations.map(s => s.results.summary),
    weeklyComparison: generateWeeklyComparisonTable(simulations)
  };
}
```

---

## ✅ 总结

这个功能模块实现后，用户可以：

1. ✅ 生成多个 Simulation，不覆盖 POR
2. ✅ 在 Library 中浏览所有 Simulation
3. ✅ 横向对比 2-3 个 Simulation，找出最优方案
4. ✅ 一键将 Simulation 转为正式 POR
5. ✅ 查看 POR 生成条件和关键指标
6. ✅ 自动对比新旧 POR 的差异
7. ✅ 追溯 POR 版本历史

**核心价值**：
- 📊 支持方案对比决策（不是盲目生成）
- 🔄 版本控制和可追溯性
- 🎯 清晰的 Simulation → POR 升级路径

---

**文档作者**: Claude Code
**基于**: 用户需求描述
**最后更新**: 2026-01-29
**状态**: ✅ 需求定义完成，待实现
